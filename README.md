# Sentiment Analyzer

Sentiment Analyzer is a full-stack project with:
- Flutter frontend (`frontend/`)
- FastAPI backend (`backend/`)
- Local DistilBERT sentiment model assets

The app accepts one or more reviews, runs sentiment classification, and shows
results in a mobile-first UI with authentication and local notifications.
